name: cinema
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  http: ^1.0.0
  provider: ^6.0.0           # gestión de estado simple
  cached_network_image: ^3.2.3
  flutter_staggered_grid_view: ^0.6.2  # opcional para layout
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0
  build_runner: ^2.3.0
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
